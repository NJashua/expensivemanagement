{
  "version": 2,
  "build": {
    "env": {
      "PYTHON_VERSION": "3.11"
    },
    "commands": {
      "build": "pip install -r requirements.txt"
    }
  },
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/app"
    }
  ]
}
